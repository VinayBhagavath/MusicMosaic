{
  "name": "musicmosaic",
  "private": true,
  "version": "0.1.0",
  "description": "Reconstruct one song from five others",
  "scripts": {
    "dev": "concurrently -k -n api,web -c cyan,magenta \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "cd backend && uv run uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "dev:web": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "test": "uv run --project backend pytest backend/tests -q",
    "postinstall": "npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
